#pragma once
#include "allHeaders.h"
int sortingByDate(Position);
int countingBills(Position);
Position* makingArray(Position, int);
int comparingDates(const void*, const void*);
int sortingTheList(Position, Position*, int);
