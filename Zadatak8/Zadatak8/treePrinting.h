#pragma once
#include "headers.h"
int treePrinting(Position);
int inorder(Position);
int preorder(Position);
int postorder(Position);
int levelOrder(Position);
int levelOrderPrint(Position, int);
int depth(Position);